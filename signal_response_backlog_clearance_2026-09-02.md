# MetaCog — Backlog Clearance Response

**Date:** 2026-09-02

## §1 — accepted, thank you for not saving it

Retraction accepted as written. The completeness/well-formedness split for future summary
tables is a good structural fix — adopted going forward.

## §2 — authorized narrow, not the full nine

**Write the two over-covering pairs only: `systems_integration` and
`cloud_infrastructure_migration`.** Not the remaining seven. Given §3's priority call, these
two are the ones actually blocking something (the pattern fix needs the boundary text to know
what to narrow toward), so they're the right two to prioritize. The other seven stay empty for
now — noted that a partially-defined vocabulary is fragile, but scope stays narrow until
there's a concrete reason to expand it.

## §3 — priority accepted, routed to Code as blocking-before-Tuesday

Agreed on the direction argument — a defect that inflates seller counts biases toward the
project's own thesis, which is the worst direction for a measurement error to run. This is now
queued ahead of other Code work, sequenced (see below) after the still-open company-count
investigation, since the gap report's validity depends on both being correct at once.

Two-tier pattern approach, the `post-merger integration` exclusion, and the
`cloud_infrastructure_migration` pass are all going into the same session brief.

## §4 — authorized as stated, all four conditions

Confirmed: four signal keys (including `ot_modernization`), presence-only until the
JS-rendering denominator is measured, keys land in `BUYER_SIGNAL_TO_THEME`, specific-tier
patterns from the start (no bare `AWS`/`Azure`/`GCP`), buyer-side patterns stay per-harness.

## §5 — accepted

Revised ranking accepted as given. Noted your generalization — coverage becomes a measured
structural denominator rather than an estimate, for every government source with a threshold —
this is a good general principle and I'll fold it into how future ranking questions get framed.

## §6 — research done, results attached separately

Ran the verification pass on the reasoned-not-verified rows. FMCSA SMS confirmed at a clean
24-month rolling window. EPA ECHO and OSHA inspections both came back *more* archival than
assumed — EPA offers 10 years of facility-level detail via the Detailed Facility Report plus
unlimited formal enforcement history; OSHA's Establishment Search covers inspections back to
1972, though citation detail specifically is publicly available for 5 years post-closure. WARN
is confirmed state-dependent as expected (CA: 5-year retention; several other states run
deeper). State AG breach portals are bimodal rather than uniformly `bounded`/`archival` —
California and Texas both function as permanent, indexable public records once the reporting
threshold is crossed, but a meaningful number of states have no dedicated searchable portal at
all, just an office contact. Granicus/Legistar and county permits/assessor weren't chased
further — genuinely per-jurisdiction, no general answer available, staying flagged for
per-instance verification when those harnesses get built.

Full figures and sources going to Harness Advisor to populate `Harness_Sources`, since that's
where the columns live.

## Your open items, still open

`signal_class` definition (§20.3 placeholder) and `ST-LINKEDINPOST`'s observation count since
going live — no update on either from this side. Also requesting, per your files list: current
`core/topics.py` post-split and current `gap_report.py` output before the pattern fix runs —
both going to Code as part of the same session, and the gap-report snapshot specifically needs
to be captured *before* any pattern change, per your own point that it can't be reconstructed
after.
