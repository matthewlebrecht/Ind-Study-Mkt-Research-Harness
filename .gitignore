# Market Intelligence Harness -- GitHub copy is CODE ONLY.
# The evidence base (the workbook and everything derived from it) stays off this repo.

# --- the database and every workbook export / backup / dump
data/market_intel_db.xlsx
*.xlsx
~$*.xlsx
*.bak-*
*.bak
data/archive/
data/snapshots/
*.csv
*.zip

# --- generated harness output: raw response archive, run logs, audit artifacts, review queues
harness_output/
harnesses/*/discovered.json

# --- secrets. `.env` holds the real API keys (Brave Search, FMCSA QCMobile, Anthropic);
#     `.env.example` is the committed template and the only env file that belongs here.
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# --- Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# --- OS / editor junk
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*~
.idea/
.vscode/
